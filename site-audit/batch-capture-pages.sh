#!/bin/bash
# Batch screenshot capture script - tracks which pages have been captured

# Already captured:
# ✓ homepage-full.png
# ✓ about-us-full.png
# ✓ why-choose-proppy.png
# ✓ how-we-do-it.png

# Remaining main pages to capture:
# - contact-us (data captured, need screenshot)
# - pricing (data captured, need screenshot)
# - what-we-do (data captured, need screenshot)
# - faq (data captured, need screenshot)
# - reviews-and-customer-testimonials (data captured, need screenshot)
# - properties-we-source (visited, need screenshot)
# - our-technology (visited, need screenshot)
# - our-results (visited, need screenshot)
# - get-started (visited, need screenshot)
# - money-back-guarantee (visited, need screenshot)
# - investor-assessment-session (visited, need screenshot)
# - investor-guides (visited, need screenshot)
# - market-updates (visited, need screenshot)
# - investor-updates (visited, need screenshot)
# - privacy-policy (visited, need screenshot)
# - terms-and-conditions (visited, need screenshot)

echo "Main Pages Progress: 4/21 captured so far"
echo "Next: Capture remaining 17 pages via Playwright"
